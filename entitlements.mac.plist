<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- App Sandbox is not enabled (the app spawns whisper.cpp/python and writes
       freely under userData), so we declare the hardened-runtime entitlements
       needed to load native modules and capture audio under signing. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- whisper.cpp / .node native modules are not signed by the same team, so
       library validation must be disabled or the app crashes at load time. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
